# Dependencies for automation/scripts/*.py — the scheduled market-data jobs.
# These run on a GitHub Actions runner, never inside the API container.
yfinance>=0.2.55
pandas>=2.2
lxml>=5.2
html5lib>=1.1
beautifulsoup4>=4.12
anthropic>=0.49
requests>=2.32
