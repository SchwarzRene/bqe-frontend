{
  "name": "bqe-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "./build.sh",
    "dev": "./build.sh && wrangler dev",
    "deploy": "./build.sh && wrangler d1 migrations apply bqe --remote && wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260924.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1",
    "wrangler": "^4.137.0"
  }
}
