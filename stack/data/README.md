Generated folder — do not edit by hand.

`scripts/fetch_market_data.py` fills this with one JSON file per ticker plus
an `index.json`. Until the first run of the **Market data** workflow, the page
falls back to its built-in simulated series and says so in the header.
